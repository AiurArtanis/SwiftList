<Window x:Class="SwiftList.Tutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:SwiftList.Tutorial.Helpers"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="550" Width="750"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        >
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Background="{StaticResource BrushBg}" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BrushBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Custom Elegant Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource BrushTitleBg}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                    <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-6h2v6zm0-8h-2V7h2v2z" 
                          Fill="{StaticResource BrushPrimary}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource BrushText}" FontWeight="Bold" FontSize="14"/>
                </StackPanel>
                <Button Grid.Column="1" Click="Close_Click" Content="✕" Foreground="{StaticResource BrushTextMuted}" Background="Transparent" BorderThickness="0" Width="40" FontSize="16" Focusable="False">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#e06c75"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Main Content Area -->
            <Grid Grid.Row="1" Margin="25,20,25,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Subtitle / Progress -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,0,10"/>
                    <Grid Height="6" Margin="0,5,0,15">
                        <Border CornerRadius="3" Background="{StaticResource BrushButtonBg}"/>
                        <ProgressBar Value="{Binding ProgressValue}" Minimum="0" Maximum="100" BorderThickness="0" Background="Transparent" Foreground="{StaticResource BrushPrimary}" Height="6">
                            <ProgressBar.Template>
                                <ControlTemplate TargetType="ProgressBar">
                                    <Grid Name="TemplateRoot">
                                        <Border CornerRadius="3" Background="{TemplateBinding Background}"/>
                                        <Border Name="PART_Indicator" CornerRadius="3" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left"/>
                                    </Grid>
                                </ControlTemplate>
                            </ProgressBar.Template>
                        </ProgressBar>
                    </Grid>
                </StackPanel>

                <!-- Interactive Tutorial Card -->
                <Border Grid.Row="1" Background="{StaticResource BrushCardBg}" CornerRadius="8" Padding="25" BorderThickness="1" BorderBrush="{StaticResource BrushBorder}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Step Title -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding StepTitle}" Foreground="{StaticResource BrushText}" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding StepCountText}" Foreground="{StaticResource BrushPrimary}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Step Description -->
                        <TextBlock Grid.Row="1" helpers:MarkdownTextHelper.Markdown="{Binding StepDesc}" TextWrapping="Wrap" Foreground="{StaticResource BrushText}" FontSize="14" LineHeight="22" Margin="0,0,0,20"/>

                        <!-- Interactive Todo List -->
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Visibility="{Binding IsTodoListVisible, Converter={StaticResource BoolToVis}}">
                            <ItemsControl ItemsSource="{Binding TodoItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderThickness="0,0,0,1" BorderBrush="{StaticResource BrushBorder}" Padding="0,10,0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center" IsEnabled="True" Margin="0,0,15,0">
                                                    <CheckBox.Style>
                                                        <Style TargetType="CheckBox">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="CheckBox">
                                                                        <Grid x:Name="checkboxGrid" Width="18" Height="18">
                                                                            <Border x:Name="checkBoxBorder" BorderBrush="{StaticResource BrushPrimary}" BorderThickness="1.5" CornerRadius="4" Background="Transparent" />
                                                                            <Path x:Name="checkMark" Visibility="Collapsed" Data="M3 9.5L7 13.5L15 5.5" Stroke="{StaticResource BrushGreen}" StrokeThickness="2" Stretch="Uniform" Margin="2"/>
                                                                        </Grid>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsChecked" Value="True">
                                                                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                                                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource BrushGreen}"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </CheckBox.Style>
                                                </CheckBox>

                                                <TextBlock Grid.Column="1" helpers:MarkdownTextHelper.Markdown="{Binding Text}" Foreground="{StaticResource BrushText}" FontSize="14" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- Bottom Navigation Bar -->
            <Grid Grid.Row="2" Background="{StaticResource BrushTitleBg}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" Click="Action_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,25,0" Width="120" Height="36" Cursor="Hand" Content="{Binding ActionButtonText}" IsEnabled="{Binding IsNextButtonEnabled}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3b82f6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{StaticResource BrushButtonBg}"/>
                                    <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
