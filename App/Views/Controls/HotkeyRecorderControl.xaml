<UserControl x:Class="SwiftList.App.Views.Controls.HotkeyRecorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="26" Width="180">
    <UserControl.Resources>
        <VisualBrush x:Key="PlaceholderBrush" TileMode="None" Stretch="None"
                     AlignmentX="Center" AlignmentY="Center">
            <VisualBrush.Visual>
                <Label Content="按下快捷键" Foreground="{DynamicResource TextSecondary}"
                       Opacity="0.5" FontSize="12"/>
            </VisualBrush.Visual>
        </VisualBrush>
        <Style x:Key="RecorderBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Background" Value="{StaticResource PlaceholderBrush}"/>
                </Trigger>
                <Trigger Property="Text" Value="{x:Null}">
                    <Setter Property="Background" Value="{StaticResource PlaceholderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <TextBox x:Name="HotkeyBox"
             Style="{StaticResource RecorderBoxStyle}"
             Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
             IsReadOnly="True"
             PreviewKeyDown="HotkeyBox_PreviewKeyDown"/>
</UserControl>
